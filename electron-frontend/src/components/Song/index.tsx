import Song, {
  SongProps,
  emptySongProps,
  songUploadOptions,
  lyricsUploadOptions,
} from './Song';

export default Song;
export { SongProps, emptySongProps, songUploadOptions, lyricsUploadOptions };
